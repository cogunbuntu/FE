import React from "react";
import LoginContainer from "../components/LoginContainer";

const LoginPage = () => {
  return (
    <div>
      <LoginContainer />
    </div>
  );
};

export default LoginPage;
